[project]
name = "void-map"
version = "0.1.0"
description = "Local Kismet kismetdb visualiser and replay tool"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
  "flask>=2.3",
  "flask-cors>=4.0",
]

[tool.uv]
package = false
